import 'package:flutter/material.dart';

class StudentFineScreen extends StatelessWidget {
  const StudentFineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
